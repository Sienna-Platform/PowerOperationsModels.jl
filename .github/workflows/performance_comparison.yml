name: 'Performance Comparison'

on:
  pull_request:

jobs:
  comparison:
    runs-on: ubuntu-latest
    steps:
      - uses: julia-actions/setup-julia@latest
      - name: Checkout PR branch
        uses: actions/checkout@v5
        with:
          path: branch
      - name: Checkout main branch
        uses: actions/checkout@v5
        with:
          ref: main
          path: main
      - name: Run Performance Test Main
        run: |
          julia --project=branch/test -e 'using Pkg; Pkg.develop(PackageSpec(path=joinpath(pwd(), "main"))); Pkg.instantiate()'
          julia -t 4 --project=branch/test branch/test/performance/performance_test.jl "Main"
      - name: Run Performance Test Branch
        run: |
          julia --project=branch/test -e 'using Pkg; Pkg.develop(PackageSpec(path=joinpath(pwd(), "branch"))); Pkg.instantiate()'
          julia -t 4 --project=branch/test branch/test/performance/performance_test.jl "This Branch"
      - name: Read precompile results
        id: precompile_results
        run: |
          body="$(cat precompile_time.txt)"
          body="${body//'%'/'%25'}"
          body="${body//$'\n'/'%0A'}"
          body="${body//$'\r'/'%0D'}"
          echo "::set-output name=body::$body"
      - name: Read build results
        id: build_results
        run: |
          body="$(cat build_time.txt)"
          body="${body//'%'/'%25'}"
          body="${body//$'\n'/'%0A'}"
          body="${body//$'\r'/'%0D'}"
          echo "::set-output name=body::$body"
      - name: Read solve results
        id: solve_results
        run: |
          body="$(cat solve_time.txt)"
          body="${body//'%'/'%25'}"
          body="${body//$'\n'/'%0A'}"
          body="${body//$'\r'/'%0D'}"
          echo "::set-output name=body::$body"
      - name: Find Comment
        uses: peter-evans/find-comment@v4
        id: fc
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: 'github-actions[bot]'
          body-includes: Performance Results
      - name: Create comment
        if: steps.fc.outputs.comment-id == ''
        uses: peter-evans/create-or-update-comment@v5
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            Performance Results
            | Version      | Precompile Time |
            | :---        |    :----:   |
            ${{ steps.precompile_results.outputs.body }}

            | Version      | Build Time |
            | :---        |    :----:   |
            ${{ steps.build_results.outputs.body }}

            | Version      | Solve Time |
            | :---        |    :----:   |
            ${{ steps.solve_results.outputs.body }}
      - name: Update comment
        if: steps.fc.outputs.comment-id != ''
        uses: peter-evans/create-or-update-comment@v5
        with:
          comment-id: ${{ steps.fc.outputs.comment-id }}
          body: |
            Performance Results
            | Version      | Precompile Time |
            | :---        |    :----:   |
            ${{ steps.precompile_results.outputs.body }}

            | Version      | Build Time |
            | :---        |    :----:   |
            ${{ steps.build_results.outputs.body }}

            | Version      | Solve Time |
            | :---        |    :----:   |
            ${{ steps.solve_results.outputs.body }}

          edit-mode: replace
